#include "Emulator.h"
#include "Log.h"
#include <string>
#include <fstream>

using namespace std;

int main(int argc, char** argv){

    string LOGPATH = "simplenes.log";
    std::ofstream logFile(LOGPATH), cpuTraceFile;


    return 0;

}
